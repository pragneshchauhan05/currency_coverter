name: Basic CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify files
        run: |
          test -f currencyapp.html
          test -f currencyapp.css
          test -f currencyapp.js
          test -f currency_code.js

      - name: Success
        run: echo "Project checked successfully"
